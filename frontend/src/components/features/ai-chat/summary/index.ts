/*
  One component for both editions. The button holds nothing pro — see its own
  notes — so there is no isPro() switch here, unlike the widget it talks to.
*/
export { default } from './ai-summary-button'
